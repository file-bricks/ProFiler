# ProFiler Suite

Canonical repository: https://github.com/file-bricks/ProFiler

ProFiler Suite is a local-first desktop file manager for private document collections. It is a Windows-oriented PySide6 application for indexing local folders, searching documents, running OCR, handling PDF workflows, detecting duplicates, and reviewing files for privacy risks before sharing.

## Audience

- Windows users managing local document archives who need full-text search without cloud upload
- Privacy-conscious users needing PDF redaction, anonymization, and pre-sharing review workflows
- Small-office workers handling scanned documents, OCR processing, and file deduplication
- Developers integrating ProSync folder sync or KnowledgeDigest LLM search into desktop workflows
- LLM agents and crawlers indexing open-source PySide6 desktop application repositories

## Primary Use

- Index local folders into a SQLite file database for fast search and filtering
- Run OCR on scanned PDFs and images via Tesseract
- Use PDF utilities for encryption, decryption, page extraction, and text removal
- Run privacy review to find potentially sensitive files before sharing or archiving
- Export a redacted workspace for reviews and cross-platform handoffs

## Important Files

- `README.md` — English overview with setup, features, and supported formats
- `README_de.md` — German overview
- `SECURITY.md` — bilingual security policy and vulnerability reporting
- `Profiler_Suite_V15.py` — main desktop application
- `ProFiler_Datenschutzampel.py` — standalone privacy traffic-light helper
- `SQLiteViewer.py` — local SQLite index viewer
- `workspace_exchange.py` — redacted workspace export/import
- `requirements.txt` — Python dependencies
- `source_platform_smoke.py` — macOS/Linux CI smoke test
- `tests/macos_platform_smoke.py` — dedicated macOS platform smoke suite
- `tests/linux_platform_smoke.py` — dedicated Linux platform smoke suite
- `PLATFORM_SUPPORT.md` — exact platform smoke evidence and boundary gates
- `version.py` — canonical public version contract (`15.0.0`)
- `STORE_LISTING.md` — bilingual Windows Store listing metadata
- `store_package.json` — Store package configuration and parameters

## Search Phrases

```
ProFiler Suite file-bricks
local-first desktop document manager Windows
PySide6 file management OCR
PDF redaction anonymization desktop app
private document archive SQLite search
OCR desktop application Tesseract Windows
file deduplication SHA-256 local
document privacy checker PySide6
desktop document search offline-first
PySide6 file manager Tesseract OCR
local file index database SQLite
```

## Disambiguation

ProFiler is not a cloud service, not a hosted SaaS document manager, not a web app, and not a general bookmark manager. It is a local Windows desktop app that keeps all data on-device in SQLite.

## Contract Boundaries

- The product interface is the desktop GUI. No ProFiler REST API, OpenAPI,
  JSON-RPC, or document-search CLI is implemented in V15.
- `github_installer.py` has its own administrative module-installer CLI; it is
  not a remote-control interface for the main application.
- The Ubuntu/macOS workflow is a six-check basis source smoke, not OCR, PDF,
  tray, packaging, or Store evidence.
- PDF passwords are session-only and are not persisted to JSON settings.

## Ecosystem Sibling Tools

Part of the **file-bricks** desktop suite under the **open-bricks** umbrella:
- `file-bricks/knowledgedigest`: Portable knowledge database with BM25 ranking & LLM summaries
- `doc-bricks/PDFtoPDFocr`: Batch OCR & searchable PDF conversion engine
- `doc-bricks/DokuZen`: Desktop PDF workshop, format converter & security unlocker
- `doc-bricks/MediaBrain`: Multi-modal media transcription & structured indexing
- `doc-bricks/TextBrain`: Document intelligence & text analysis
- `dev-bricks/DevCenter`: Developer workspace dashboard & automation launcher
- `dev-bricks/CodeBox`: Offline snippet vault & code runner sandbox
- `ellmos-ai/ellmos-filecommander-mcp`: Safe, sandboxed MCP file management & batch processing
- `ellmos-ai/ellmos-codecommander-mcp`: MCP code intelligence, AST analysis & format repair
- `dev-bricks/sqlite-transit-sync`: Zero-loss multi-master SQLite replication & synchronization

Test status: 148/148 Python tests passing (100% green).

## Last-checked: 2026-08-24
